# Workflows

## Version control

The user manages Git commits. Coding agents should:
- Complete work as a changelist for user review
- Do not commit to Git after finishing a task (unless explicitly instructed)
- Do not push to remote (unless explicitly instructed)
